-r requirements.txt

# Optional direct local-model backend. Keep these bounded so installing the
# backend cannot silently cross a major API boundary.
torch>=2.6.0,<3
transformers>=4.52.3,<5
accelerate>=1.1.0,<2
